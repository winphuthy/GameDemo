using System.Collections;
using System.Collections.Generic;
using System.Linq;
using UnityEngine;

public class Testing : MonoBehaviour
{
    Navigation N;
    Initalization init;

    private Unit u;
    // Start is called before the first frame update
    void Start()
    {
        /*N = FindObjectOfType<Navigation>();
        List<Tile> corner1 = N.neighbour(N.GetTile(new Vector2Int(0, 0)));
        List<Tile> corner2 = N.neighbour(N.GetTile(new Vector2Int(9, 0)));
        List<Tile> corner3 = N.neighbour(N.GetTile(new Vector2Int(0, 9)));
        List<Tile> corner4 = N.neighbour(N.GetTile(new Vector2Int(9, 9)));
        
        foreach (var tile in corner1)
        {
            tile.Highlight();
        }
        
        foreach (var tile in corner2)
        {
            tile.Highlight();
        }
        foreach (var tile in corner3)
        {
            tile.Highlight();
        }
        foreach (var tile in corner4)
        {
            tile.Highlight();
        }
        */




    }

}
